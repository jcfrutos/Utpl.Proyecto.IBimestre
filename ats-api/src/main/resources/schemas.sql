CREATE TABLE ats_compras (
    id SERIAL PRIMARY KEY,
    campo1 VARCHAR(255),
    campo2 VARCHAR(255),
    -- Agregar más campos según la estructura del archivo XML
);

CREATE TABLE ats_ventas (
    id SERIAL PRIMARY KEY,
    campo1 VARCHAR(255),
    campo2 VARCHAR(255),
    -- Agregar más campos según la estructura del archivo XML
);

CREATE TABLE ats_rendimientos (
    id SERIAL PRIMARY KEY,
    campo1 VARCHAR(255),
    campo2 VARCHAR(255),
    -- Agregar más campos según la estructura del archivo XML
);

CREATE TABLE ats_anulados (
    id SERIAL PRIMARY KEY,
    campo1 VARCHAR(255),
    campo2 VARCHAR(255),
    -- Agregar más campos según la estructura del archivo XML
);

CREATE TABLE ats_recaps (
    id SERIAL PRIMARY KEY,
    campo1 VARCHAR(255),
    campo2 VARCHAR(255),
    -- Agregar más campos según la estructura del archivo XML
);